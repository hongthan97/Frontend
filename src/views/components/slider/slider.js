const Slider = {
    showSlider: () => {
        $('.single-item').slick({
            infinite: false,
            // slidesToShow: 1,
            // speed: 300,
        });
    },
};
export default Slider;
